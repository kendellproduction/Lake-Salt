# ── Logs ──────────────────────────────────────
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── Dependencies ───────────────────────────────
node_modules
dist
dist-ssr
*.local

# ── Editor ─────────────────────────────────────
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Media ──────────────────────────────────────
static/images/*.MOV

# ── SECRETS — never commit these ───────────────
.env
.env.*
!.env.example
website/firebase-config.js
*.secret
*.secrets
secrets.json
service-account.json
*-service-account.json
firebase-adminsdk*.json
google-credentials.json
lake-salt-calendar-key.json
lakesalt-*.json
.firebase/
firebase-debug.log
firestore-debug.log

# ── Firebase hosting cache ─────────────────────
.firebase/

# Gmail API OAuth (post-expo cohort drafts script)
scripts/.gmail-token.json
scripts/.gmail-credentials.json

# ── Agent runtime files (live in this folder, not in git) ──
.lake-salt-comms-state.json
lake-salt-comms-state.json
comms-check-state-*.json
.scheduled-state/
managed-agents/
lake-salt-autonomous-agent/
comms-agent-upgrade/
comms-hub-build/
output/
*.plugin
.writetest
**/.writetest
AGENT-RUNBOOK-*.md
START-HERE-comms-hub.md
DEPLOY-COMMS-HUB.sh
